import { FormatInfinityPipe } from './format-infinity.pipe';

describe('FormatInfinityPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInfinityPipe();
    expect(pipe).toBeTruthy();
  });
});
